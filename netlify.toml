[build]
  command = "npm run build"
  publish = "build"
  functions = "netlify/functions"

# Friendlier path for the contact form function
[[redirects]]
  from = "/api/contact"
  to = "/.netlify/functions/contact"
  status = 200
